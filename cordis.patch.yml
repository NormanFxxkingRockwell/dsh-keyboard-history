# dsh-keyboard-history bundle layer — 把插件插进宿主组合。
# 安装后由 dsh 自动应用；本文件的 insert 行按包名引用插件，
# Node 模块解析会定位到已安装的 dsh-keyboard-history 包。
- insert:
    - id: dsh-keyboard-history
      name: dsh-keyboard-history